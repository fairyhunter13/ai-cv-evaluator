apiVersion: 1

policies:
  - orgId: 1
    receiver: email-ai-cv-evaluator
    # Group by common alert labels so related alerts are batched.
    group_by: ['alertname', 'severity']
    matchers: []
    object_matchers: []
    mute_time_intervals: []
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
